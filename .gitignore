__pycache__/
*.pyc

# no patient-level data should ever live in this repo
*.parquet
*.csv
*.xlsx
*.zip
# ...except the synthetic example patients for the demo
!examples/*.csv

# model artefacts are derived from the access-restricted OAI data — do NOT publish
# them in this PUBLIC repo. Build locally with backend/train_model.py.
backend/model.joblib
backend/meta.json

# staging folder for sharing the model with the team (Google Drive) — contains
# model.joblib, must never be committed to this public repo
share-bundle/
